// Misma tarjeta que OpenGraph: X y WhatsApp leen etiquetas distintas pero la
// imagen es la misma, así que no hay motivo para mantener dos archivos.
export const runtime = "edge";

export { default, alt, size, contentType } from "./opengraph-image";
